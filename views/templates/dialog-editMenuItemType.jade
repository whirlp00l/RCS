material-dialog.width-md(id="dialog-editMenuType")
  .dialog-content.height-md
    h4 更改菜品<b>{{name}}</b>类别:
    material-radio-group(ng-model="selectedType")
      material-radio-button(
        ng-repeat="type in types"
        value="{{type}}" aria-label="{{type}}")
        h4 {{type}}

  .dialog-actions(layout="horizontal")
    material-button.material-button-raised.material-theme-light(
      ng-click="clickUpdateType()") 确认更改
    material-button.material-button-colored(
      ng-click="clickCancel()") 取消